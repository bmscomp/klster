# Proxy Configuration
# Add your proxy settings here in KEY=VALUE format
# Example:
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1,.svc,.cluster.local
